import styles from './Cookies.module.scss';

const Cookies = () => {
	return <div className={styles.cookies}>This is a cookie free zone! 🍪 No, not those cookies... the other ones.</div>;
};

export default Cookies;
